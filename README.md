## Sample run

To run the PMF model with the "sgd" configuration and handle cold-start cases using the median fallback strategy, use the following command:

```bash uv run pmf_main.py --discrete --output_suffix test ```